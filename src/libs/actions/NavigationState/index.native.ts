// In native, the lastVisitedPath is always initialized to an empty string.
// So, we don't need to save and restore the navigation state.
function getLastNavigationState() {}

function updateLastNavigationState() {}

export {getLastNavigationState, updateLastNavigationState}; 
